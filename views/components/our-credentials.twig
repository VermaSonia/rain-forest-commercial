{% for page in field.meta('comp') %}
	{% if page.acf_fc_layout == 'credentials' %}
		<section class="our-credentails-section padding light-bg" id="our-credentails-section">
			<div class="container">
				<h2 class="charcol title center">{{page.title}}</h2>
				<div class="slider-container section-small-top">
					<div class="logo-slider">						
                        {% for list in page.logo %}
							<div class="item-list">
								<img src="{{get_image(list.image).src}}" alt="{{get_image(list.image).alt}}" class="img-responsive">
							</div>
						{% endfor %}
                        {% for list in page.logo %}
							<div class="item-list">
								<img src="{{get_image(list.image).src}}" alt="{{get_image(list.image).alt}}" class="img-responsive">
							</div>
						{% endfor %}
                        {% for list in page.logo %}
							<div class="item-list">
								<img src="{{get_image(list.image).src}}" alt="{{get_image(list.image).alt}}" class="img-responsive">
							</div>
						{% endfor %}
					</div>
				</div>
			</div>
		</section>
	{% endif %}
{% endfor %}
