{% extends "base.twig" %} 

{% block content %}
    <section class="{{field.slug}}-page padding" id="{{field.slug}}-page">
        <div class="container">
            {{field.meta('terms_here') }}
        </div>
    </section>

{% endblock %}